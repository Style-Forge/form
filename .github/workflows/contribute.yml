name: Create PR from Contributions

on:
  push:
    branches:
      - feature/*

permissions:
  contents: write
  pull-requests: write

jobs:
  create-pr:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: Get Git user info
        id: git_info
        run: |
          COMMIT_AUTHOR_NAME=$(git log -1 --pretty=format:'%an')
          COMMIT_AUTHOR_EMAIL=$(git log -1 --pretty=format:'%ae')
          if [ "${COMMIT_AUTHOR_NAME}" = "Sarmaged" ]; then
            echo "GIT_USER_NAME=github-actions" >> $GITHUB_ENV
            echo "GIT_USER_EMAIL=github-actions@github.com" >> $GITHUB_ENV
          else
            echo "GIT_USER_NAME=${COMMIT_AUTHOR_NAME}" >> $GITHUB_ENV
            echo "GIT_USER_EMAIL=${COMMIT_AUTHOR_EMAIL}" >> $GITHUB_ENV
          fi

      - name: Configure Git
        run: |
          git config --global user.name "${{ env.GIT_USER_NAME }}"
          git config --global user.email "${{ env.GIT_USER_EMAIL }}"

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "Prepare for PR from feature branch"
          branch: ${{ github.ref }}
          base: main
          title: "PR: Changes from feature branch"
          body: "This PR contains the changes from commit ${{ github.sha }} on feature branch."
          labels: enhancement
